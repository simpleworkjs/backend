<% layout('layout') -%>
<div class="sw-collection-mount" data-sw-model="<%= modelName %>" data-sw-mode="collection">
  <!-- Replaced by app.render.js with the collection card (header actions,
       list body with edit/delete, paginated footer). -->
  <div class="alert alert-info">Loading <%= schema.display.name %>…</div>
</div>

<script>
  app.model.ready(function(){
    app.render.build('[data-sw-model="<%= modelName %>"]');
  });
</script>
